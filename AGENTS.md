# Agent Prompts for Gematria Libraries

This file is to assist AI agents to populate the gematria libraries required for this project and the calculation of The Ruach Action and Perception Equations.

---

## `A_library.json` (Adeptship)

**Task:** Populate the `A_library.json` file.

This library represents the concept of **Adeptship**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Adeptship.

**Core Concepts:**
*   Mastery, skill, expertise
*   Proficiency, craftsmanship, high competence
*   Being an expert, master, or a skilled artisan/practitioner
*   Words like "master," "expert," "chief," "head," "skilled," "wise-hearted"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of high skill and mastery.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "חכם",
    "transliteration": "chakam",
    "gematria": 72
  }
]
```

---

## `alpha_library.json` (Skill Constant)

**Task:** Populate the `alpha_library.json` file.

This library represents the concept of the **Skill Constant**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Skill.

**Core Concepts:**
*   Skill, craft, workmanship
*   Artistry, cleverness, technical ability
*   The quality of being skillful or well-made
*   Words like "skill," "work," "deed," "craft," "cunning work"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of skill and craft.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "מלאכה",
    "transliteration": "melakah",
    "gematria": 96
  }
]
```

---

## `kappa_library.json` (Efficiency Constant)

**Task:** Populate the `kappa_library.json` file.

This library represents the concept of the **Efficiency Constant**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Efficiency.

**Core Concepts:**
*   Speed, quickness, haste
*   Precision, directness, effectiveness
*   Completing a task without waste (of time, energy, or materials)
*   Words like "swift," "quick," "diligence," "straight," "upright," "success"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of efficiency and effectiveness.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "צלח",
    "transliteration": "tsalach",
    "gematria": 128
  }
]
```

---

## `N_library.json` (Collective Force)

**Task:** Populate the `N_library.json` file.

This library represents the concept of **Collective Force**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to groups of people acting as a single unit.

**Core Concepts:**
*   Assembly, congregation, multitude
*   Nation, people, tribe, family
*   Army, host, troop, company
*   Unity, gathering, community
*   Words like "nation," "people," "assembly," "host," "all," "together"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus on terms that represent a collective or a unified group.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "עדה",
    "transliteration": "edah",
    "gematria": 79
  }
]
```

---

## `k_library.json` (To Know)

**Task:** Populate the `k_library.json` file.

This library represents the concept of **To Know**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Knowledge and Knowing.

**Core Concepts:**
*   Knowledge, understanding, wisdom
*   The act of knowing, perceiving, or seeing (in the sense of understanding)
*   Insight, discernment, information
*   Words like "to know," "knowledge," "wisdom," "understanding," "to see," "to perceive"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of knowing and knowledge.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "ידע",
    "transliteration": "yada",
    "gematria": 20
  }
]
```

---

## `d_library.json` (To Dare)

**Task:** Populate the `d_library.json` file.

This library represents the concept of **To Dare**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Daring and Courage.

**Core Concepts:**
*   Bravery, courage, valor
*   The act of daring, challenging, or testing
*   Venturing into danger; boldness, fearlessness
*   Words like "courage," "strong," "mighty man," "to be bold," "to not be afraid"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of daring and courage.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "חזק",
    "transliteration": "chazaq",
    "gematria": 115
  }
]
```

---

## `w_library.json` (To Will)

**Task:** Populate the `w_library.json` file.

This library represents the concept of **To Will**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Will and Volition.

**Core Concepts:**
*   Will, desire, purpose, intent
*   The act of choosing, consenting, or being willing
*   Delight, pleasure (in the sense of "it is my will/pleasure")
*   Words like "will," "desire," "to choose," "to be willing," "purpose," "heart" (in the context of intent)

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of will and volition.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "רצון",
    "transliteration": "ratson",
    "gematria": 346
  }
]
```

---

## `s_library.json` (To Keep Silent)

**Task:** Populate the `s_library.json` file.

This library represents the concept of **To Keep Silent**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Silence and Secrecy.

**Core Concepts:**
*   Silence, quiet, stillness
*   The act of being silent, keeping secret, or hiding something
*   Rest, peace, calm
*   Words like "silence," "to be silent," "stillness," "secret," "to hide," "peace"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of silence and stillness.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "דמם",
    "transliteration": "damam",
    "gematria": 84
  }
]
```

---

## `I_library.json` (Imagination)

**Task:** Populate the `I_library.json` file.

This library represents the concept of **Imagination**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Imagination.

**Core Concepts:**
*   The act of forming mental images, devising, or planning
*   Visions, dreams
*   The "inclination" or "imagination of the heart"
*   Words like "imagination," "form," "device," "thought," "vision," "dream"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of imagination and creative thought.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "יצר",
    "transliteration": "yetser",
    "gematria": 290
  }
]
```

---

## `F_library.json` (Faith)

**Task:** Populate the `F_library.json` file.

This library represents the concept of **Faith**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Faith.

**Core Concepts:**
*   Belief, trust, reliance
*   Faithfulness, firmness, steadfastness
*   Hope, confidence, security (in a divine context)
*   Words like "faith," "to believe," "to trust," "faithfulness," "hope," "Amen"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of faith and trust.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "אמונה",
    "transliteration": "emunah",
    "gematria": 102
  }
]
```

---

## `D_library.json` (Disturbance)

**Task:** Populate the `D_library.json` file.

This library represents the concept of **Disturbance**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Disturbance.

**Core Concepts:**
*   Chaos, turmoil, confusion, trouble
*   Shaking, trembling, quaking (of the earth or people)
*   Noise, tumult, uproar
*   Storm, tempest, whirlwind
*   Words like "trouble," "tremble," "rage," "noise," "storm," "terror," "confusion"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus on terms that represent states of disorder, chaos, and disturbance.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "רגז",
    "transliteration": "rogaz",
    "gematria": 209
  }
]
```

---

## `Delta_library.json` (Diaphane)

**Task:** Populate the `Delta_library.json` file.

This library represents the concept of **Diaphane**, meaning transparency and clarity of perception. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to this idea.

**Core Concepts:**
*   Clarity, purity, brightness
*   Transparency (like clear glass or water)
*   Light, splendor, shining
*   Being clean, pure, or without blemish
*   Words like "clear," "pure," "light," "bright," "to shine," "crystal," "clean"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of clarity, purity, and transparency.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "זכך",
    "transliteration": "zakak",
    "gematria": 31
  }
]
```

---

## `R_library.json` (Resonance)

**Task:** Populate the `R_library.json` file.

This library represents the concept of **Resonance**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Resonance, in the sense of a sympathetic vibration, a response, or a deep connection.

**Core Concepts:**
*   Responding, answering, calling back
*   Echoing, resounding sound
*   Sympathy, empathy, having a heart-to-heart connection
*   Singing together, shouting in unison
*   Words like "to answer," "voice," "sound," "shout," "song," "heart," "to call"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus on terms that represent a response, a shared vibration, or a deep connection.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "ענה",
    "transliteration": "anah",
    "gematria": 125
  }
]
```

---

## `Sigma_library.json` (Signal Integrity)

**Task:** Populate the `Sigma_library.json` file.

This library represents the concept of **Signal Integrity**, meaning the quality, truth, and wholeness of a message or signal. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to this idea.

**Core Concepts:**
*   Truth, faithfulness, reliability
*   Integrity, wholeness, completeness, perfection
*   The quality of a clear, true, and uncorrupted message
*   Words like "truth," "faithful," "perfect," "whole," "upright," "just," "right"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of truth, integrity, and signal quality.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "אמת",
    "transliteration": "emeth",
    "gematria": 441
  }
]
```

---

## `Lambda_library.json` (Astral Noise)

**Task:** Populate the `Lambda_library.json` file.

This library represents the concept of **Astral Noise**, meaning spiritual or perceptual interference, falsehood, and deception. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to this idea.

**Core Concepts:**
*   Falsehood, lies, deception, vanity
*   Idolatry, false gods, divination, sorcery
*   Empty or worthless things
*   Confusion, meaningless speech
*   Words like "lie," "falsehood," "vanity," "idol," "sorcery," "worthless," "deceit"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus on terms that represent spiritual noise, falsehood, and perceptual interference.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "שוא",
    "transliteration": "shav",
    "gematria": 307
  }
]
```

---

## `beta_library.json` (Sensitivity Exponent)

**Task:** Populate the `beta_library.json` file.

This library represents the concept of the **Sensitivity Exponent**. Your task is to search the entire Hebrew Tanach for words and multi-word phrases that are semantically related to Sensitivity and Receptiveness.

**Core Concepts:**
*   The ability to feel, perceive, or receive subtle information
*   Being tender, soft, or open (especially the heart)
*   Listening, hearkening, paying close attention
*   Words like "to listen," "to hear," "heart" (in the context of feeling), "tender," "to incline the ear"

**Instructions:**
1.  Identify relevant Hebrew words and phrases from the Tanach.
2.  For each term, provide the original Hebrew and its transliteration.
3.  Calculate the standard Gematria value for each Hebrew term.
4.  Focus only on terms that positively represent the concept of sensitivity and receptiveness.
5.  Format the final output as a JSON array of objects, where each object contains `hebrew`, `transliteration`, and `gematria` keys.

**Output Format Example:**
```json
[
  {
    "hebrew": "אזן",
    "transliteration": "azan",
    "gematria": 58
  }
]
```

---
*Note: The libraries for `chi_prime`, `Psi`, `D_opt`, and `sigma` are likely derived from other parameters or are mathematical constructs rather than direct thematic concepts. They are included here for completeness but may not be populated in the same way.*
